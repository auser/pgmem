{
  "name": "@auser/pmem",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "run-s build:package build:ts",
    "build:package": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "build:ts": "tsbb build --file-names index.ts --emit-types --esm dist/esm --cjs dist",
    "postbuild:ts": "cp ./index.node dist/index.node && cp ./index.node dist/esm/index.node",
    "build-debug": "npm run build --",
    "build-release": "npm run build:package -- --release",
    "install": "npm run build-release",
    "test": "jest --runInBand --forceExit",
    "clean": "neon clean && rm -rf ./node_modules"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.2.2",
    "cargo-cp-artifact": "^0.1.6",
    "jest": "^29.2.2",
    "npm-run-all": "^4.1.5",
    "tsbb": "^3.7.8"
  }
}
